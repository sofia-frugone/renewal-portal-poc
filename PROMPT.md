# Session Prompt — Change "See plan →" to "Select plan →"

> Read CLAUDE.md before writing any code.

---

## Task

In `src/pages/YourPlan.tsx`, find the upgrade nudge strip text link that reads `See plan →` and change it to `Select plan →`.

That's the only change. Do not touch any other file.
